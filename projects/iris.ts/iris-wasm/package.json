{
    "name": "@yydb/iris-wasm",
    "private": true,
    "version": "0.1.0",
    "description": "Private wasm-pack build workspace for @yydb/iris (not published)",
    "license": "MPL-2.0",
    "type": "module",
    "scripts": {
        "build": "node ./scripts/build.mjs --release",
        "build:debug": "node ./scripts/build.mjs"
    },
    "engines": {
        "node": ">=18"
    }
}
