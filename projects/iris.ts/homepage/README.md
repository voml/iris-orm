# Iris homepage (`@yydb/iris-homepage`)

Official Iris ORM site — [VMZ](https://github.com/vmz-framework/vmz-framework) **0.1.8** stack (`@vmz/core`, `@vmz/ui`, `@vmz/plugin-markdown-it`).

```bash
# from iris-orm repo root
pnpm install
pnpm --filter @yydb/iris-homepage dev
pnpm --filter @yydb/iris-homepage build
```

- Landing: `/`
- Documents: `/d/zh-hans/` · `/d/en-us/`

Product messaging follows the Rust-core + N-API binding model. Legacy TypeScript `iris-adapter-*` packages are not part of the public surface.
